---
- hosts: all
  become: yes
  tasks:
   - name: Create deployment directory
     file:
       path: /tmp/cicd-demo
       state: directory
   - name: Copy latest deployment file
     copy:
       src: deployment.txt
       dest: /tmp/cicd-demo/deployment.txt
       force: yes
